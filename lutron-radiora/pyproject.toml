[project]
name = "gilbert-plugin-lutron-radiora"
version = "1.0.0"
description = "Lutron RadioRA 2 / HomeWorks integration for Gilbert"
requires-python = ">=3.12"
dependencies = [
    "pylutron>=0.4.1",
]

[tool.uv]
package = false
