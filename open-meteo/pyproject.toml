[project]
name = "gilbert-plugin-open-meteo"
version = "1.0.0"
description = "Open-Meteo weather backend for Gilbert"
requires-python = ">=3.12"
# Uses ``httpx`` which is already a Gilbert core dep — no extra packages needed.
dependencies = []

[tool.uv]
package = false

