[project]
name = "gilbert-plugin-kokoro"
version = "1.0.0"
description = "Kokoro local TTS backend for Gilbert"
requires-python = ">=3.12"
dependencies = [
    "kokoro>=0.9",
    "torch>=2.4",
    "av>=12",
    "numpy>=1.26",
]

[tool.uv]
package = false
